- Make Administrator Top filter format on per node pages
- Create Primary and Secondary Menu Blocks
- make admin user have administer role
- Add CSS defaults page
- Create and set home page node.
- Arrange content type fields so that menu is not at top
- add cache router and APC modules
